# 进度

## 已完成

- 已完成现有 session kit、插件权限和宿主能力分发调查。
- 已确定完整读取与带备份写入的接口契约。
- 已实现 session kit 完整读取与受保护写入 API。
- 已注册 `synthv.session/read|write`，并通过 `host.internal` 双层授权。
- 已补充插件 SDK 类型和宿主合同检查。
- 已合并账号明细、完整明文编辑、布局修复和离线步骤弹窗。
- 离线切换使用后端真实阶段事件，显示预检、备份、远端请求、本地更新和回读状态；失败会保留出错阶段及消息。
- 账号设置可以显示完整 access、refresh、时间、设备、用户、产品字段、session 路径与密文哈希。
- UI 写入要求当前密文哈希，写入前自动创建不可覆盖备份，完成后显示新哈希与备份路径。
- 生产前端构建、全量 Node 合同测试及严格 Rust Clippy 均通过。
- 使用本地预览和合成 session 完成视觉检查，确认文字正常排版、明细完整显示和五阶段弹窗逐项完成。
- 所有自动验证只使用临时目录和合成凭据，没有访问真实 SV2 session。
