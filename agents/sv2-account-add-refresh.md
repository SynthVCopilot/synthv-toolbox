# 新增账号后保留授权声库显示

## 现象

新增或导入账号槽位后，界面会立即采用变更接口返回的基础槽位状态。旧槽位此前已识别的授权声库可能尚未在该状态中重新确认，导致可用授权列表暂时显示为空。

## 决策

新增或导入完成后，若用户已开启账号登录指示器，立即执行一次既有的账号预检快照刷新。这样返回的槽位集合包含当前识别结果，并维持原有的授权与登录状态展示；未开启时，现有刷新函数保持无网络调用。

## 验证

- 根目录契约测试覆盖新增与导入后的刷新调用。
