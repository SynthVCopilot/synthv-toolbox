# 过期会话刷新审计

- [x] 合并任务基线并审查活动和空闲探测路径。
- [ ] 统一过期会话刷新、CAS 写回和只读授权读取。
- [ ] 覆盖活动/空闲顺序、一次重试、文件变化和失败边界。

- 活动环境的近过期会话先刷新、CAS 写回后读取新 token 的授权；新鲜 token 只有在授权拒绝时刷新并重读一次。空闲批处理同样在首次授权拒绝后进入既有单次刷新和 CAS 路径，过期会话不先读取授权。
