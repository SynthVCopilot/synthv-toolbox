# 调研记录

- [现状] -> [检查 Git 子模块与引用] -> `external/synthv-agent-bridge` 是唯一仍受跟踪的子模块；Tauri 配置和运行时开发路径均直接引用它。
- [保留性] -> [检查子模块工作树] -> 子模块包含本地未提交修改，迁移必须以当前工作树为源，不能重置或检出该子模块。
- [完整性] -> [对比迁移前后的 SHA-256] -> 排除可再生的 `node_modules`、`dist` 等目录后，132 个源文件全部逐字节一致。
- [构建] -> [运行组件检查、桌面构建与契约测试] -> Bridge TypeScript 检查及 250 项测试通过；桌面前端构建和所有契约测试通过。
- [Rust] -> [运行 `cargo check`] -> 已通过 Tauri 组件资源验证，但现有 `synthv_control.rs` 使用的 `windows-sys` 类型与锁定版本不兼容；本次迁移未修改该文件。
- [上游同步] -> [变基遇到子模块更新] -> 将远程最新 Bridge 版本与迁移前的 15 个本地修改做三方合并，全部自动合并；SV1 Legacy 与统一宿主支持一并保留。
- [组件测试] -> [运行 SV1 Lua host 测试] -> 未安装 Lua 的开发机曾将解释器缺失报告为断言失败；测试现在明确跳过该环境，保持 CI 中已安装 Lua 的实际验证。
- [最终验证] -> [组件检查、桌面构建和契约测试] -> Bridge 258 项测试通过（65 项在无 Lua 环境跳过），桌面生产构建与全部契约测试通过；`cargo check` 仍受既有 `windows-sys` 类型错误阻断。
