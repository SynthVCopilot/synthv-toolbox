# SynthV Agent Bridge v3 修复、开发与真机复测证据

> - 执行时间：2026-07-31 14:45–23:40（Europe/Berlin）
> - 分支：`codex/v3-implementation`
> - 基线 Git commit：`96f603616c71d801ffb99dd883ebd6708190dc98`
> - 产品版本：`0.2.0`；文件 IPC：仅 `v3`
> - 环境：Windows 11、Synthesizer V Studio 2 Pro `2.2.1` standalone、Node `v24.18.0`、npm `11.16.0`、Lua `5.4.8`
> - 发布结论：**发布收缩后的稳定版 `0.2.0`。人工听感、Stage 1–2、Stage 3 功能/重复性和用户批准的一小时长稳（200 writes、3,400 reads、10 reloads）通过；7 条已复现宿主风险的路径继续标为 experimental 并在工程 IPC 前失败关闭。修复后的资源监控一小时重跑由用户明确取消和豁免，本报告记为 `WAIVED / NOT PASS`，不把豁免表述为通过。**

本报告接续 [14:12–14:24 测试证据](v3-test-evidence-2026-07-31-1424.zh-CN.md)，记录本轮“测试 → 诊断 → 修复/开发 → 安装 → SynthV 真机复测 → 恢复基线”的结果。报告不保存原始 Group UUID、fingerprint、Guard、歌词/音符数组或 Vocal 身份声明。

## 1. 最终实现改动

| 范围 | 缺陷或风险 | 最终处理 | 验证 |
|---|---|---|---|
| Query 隐私 | `get_track_notes` 嵌套 Track 暴露 main Group locator | 公共投影删除私有 locator | 默认、显式字段、debug、真机路径和回归均通过 |
| Doctor | 注入 Build ID 后的安装 Lua/Sidebar 被稳定误报不一致 | Doctor 复用正式 Component Build Identity 准备逻辑 | prepared 安装回归和真实安装目标检查通过 |
| Sidebar | Windows 原子状态替换偶发 `EBUSY/EACCES/EPERM` | 有界退避重试；状态增加写入 PID | 竞争注入回归及 Sidebar Preview/Apply/Undo 真机通过 |
| Command 隐私 | 成功确认和错误细节可能携带私有 UUID | 最小确认不复制 `*Uuid`；错误统一脱敏 `uuid/guardToken` | 自动化及 linked clone/shared-write 真机复测通过 |
| UI 隐私 | `set_selection` 宿主回读未复用 `get_selection` 隐私投影 | 选择写入和读取使用同一 Context/Guard 压缩及私有字段删除 | 自动化与真机选择/回读/清空均无 UUID/Guard 泄漏 |
| Clone/Transaction 安全 | 不稳定的宿主 clone/transaction 路径仍可从公共接口调用 | 新增可发现的实验性稳定性注册表，在 IPC 前失败关闭 | 7 条危险请求真机均返回 `EXPERIMENTAL_CAPABILITY_DISABLED`、`wrote=false`、`undoRequired=false` |
| Harmony 绕行 | `create_harmony_track` 内部绕过被禁用的 `clone_track` | 纳入同一实验性禁用集合 | 自动化与真机写前失败关闭通过 |
| Track no-op | `update_track` 同值写入仍产生 Undo | 写前比较；0 变化返回 `alreadySatisfied`；写后权威回读 | Fake Host 与真机同值 0 Undo，实际改名 1 Undo，菜单 Undo 恢复 |
| Group no-op | `update_group` 同值 reference-local 写入仍产生 Undo | 比较 name/mute/timeOffset/pitchOffset；写后权威回读 | Fake Host 与真机同值 mute 0 Undo，实际 mute 1 Undo，菜单 Undo 恢复 |
| Time Axis no-op | `set_time_axis` 同值 tempo mark 仍产生 Undo | 在 clone 上应用候选操作并比较完整 time-axis fingerprint；相同则 0 Undo | Fake Host 与真机 120 BPM 同值 0 Undo；新增 121 BPM 1 Undo；菜单 Undo 恢复 |
| Metadata no-op | `script_data` 重复 set、删除不存在键仍产生 Undo | JSON 值等价比较；无变化 0 Undo；set/remove 后权威回读 | Fake Host 与真机 set/remove 各 1 Undo，重复 set/remove 各 0 Undo；两次菜单 Undo 恢复 |
| Metadata 路由 | `script_data get/list` 通过 `sv_command` 被伪报为 changed/1 Undo | 公共 v3 `sv_command` 只接受 set/remove；`sv_describe` 同步收缩 schema | 自动化确认 get/list 在项目 IPC 前失败关闭；真实 set/remove 保持可用 |
| Track 删除 | `delete_track` 缺少集合数量和顺序后置条件 | 校验只删除一个 Track，并校验相邻 Track fingerprint/顺序；失败要求一次 Undo | Fake Host 故障注入和真机 add/delete/readback/两次 Undo 通过 |
| Reference 删除 | `delete_group_reference` 缺少集合数量和顺序后置条件 | 校验只删除一个 Reference，并校验相邻 Reference fingerprint/顺序；失败要求一次 Undo | Fake Host 故障注入和真机 delete/readback/Undo 通过 |
| Reference 增加 | `add_group_reference` 尚无本轮真机证据 | 使用 fresh library Group Context，在同一 Track 增加 linked Reference | 1 Undo；Track Group count 2→3、library reference count 1→2；菜单 Undo 后恢复 2/1 |
| Library Group | create/delete 缺少集合数量、身份和顺序后置条件 | 对创建和删除都校验 library count、UUID、插入/删除位置及 survivor 顺序 | Fake Host 全流程和真机 create/delete/readback/两次 Undo 通过，未复现原生崩溃 |
| Local score import | 尚无本轮真机写入证据 | 显式绝对路径、SHA-256、单声部选择、rights confirmation、fresh Group Context | MusicXML 导入 2 notes、1 Undo；source 90 BPM 未应用到项目 120 BPM；Undo 后恢复 3 notes 与 gaps `[0,0]` |
| Note add | `add_notes` 尚无当前构建的独立真机写入/恢复证据 | 在本任务创建的 disposable Group 尾部添加一个严格接续的合成 note | changed=1、Undo=1、独立回读为 4 notes 且 gaps `[0,0,0]`；可见菜单 Undo 后恢复 3 notes、gaps `[0,0]`，标题脏标记随后清除 |
| Evidence model | API 覆盖清单无法表达发布设计要求的 `experimental/unsupported` 终态，且 UI 仍名为 `sampledUi` | 覆盖检查器接受 Stage 2 终态，UI 改为 `verifiedUi`，并强制实时 capability-stability registry 与文档的 experimental 状态一致 | 先红后绿：旧检查器准确拒绝新状态；修复后 64/64 Actions、38 writes、0 errors；完成调音复测后为 31 verified、7 experimental、0 pending |
| 文档漂移 | README/roadmap/changelog/开发计划/性能与测试矩阵仍混有 v2、迁移进行中或旧 clone/transaction 认证结论 | 更新为六工具 v3、Phase 0–8 完成、当前 Alpha 安全收缩和最新 Node 24 证据；clone URL 与实际 origin 一致 | `rg` 不再发现错误的 v2 运行时/迁移进行中表述；API coverage 与当前稳定性注册表一致 |
| Stage 3 可执行性 | 发布设计只有文字计数，没有可重复运行且能自动执行停止条件的 read matrix | 新增 `validate:v3-reads`：精确轮转 17 Query、默认生成 1,000-call 计划；逐次校验工程/Session/executor，强制 20 KB 预算，自动删除合成 MusicXML probe；Stage 2 前拒绝完整 live matrix | dry-run、Session 漂移、超预算和前置门禁测试通过；17-call 真机 development smoke 完成 17/17，最大响应 1,549 B、p95 48.057 ms；明确不计为 Stage 3 通过 |
| Stage 4 安装交付 | 自动化已有干净安装和注入替换失败回滚，但缺少旧三组件完整集合成功升级 | 增加临时目录内的 Bridge/Stop/Sidebar 原子升级回归，校验三个旧文件全部替换、Build ID 注入且无 staging/backup 残留 | 定向 install suite 6/6 通过：clean/core-only、offline、完整升级、注入失败完整回滚均覆盖；真实 SynthV 安装目标 Doctor 仍匹配当前构建 |

公共稳定性收缩：

- `clone_group_reference(linked)` 仍可用；`isolated` 为实验性禁用；
- 完全实验性禁用：`clone_note_group`、`clone_track`、`clone_track_shell`、`create_harmony_track`、`apply_transaction`、`rollback_transaction`；
- `sv_describe` 保留 schema、可用性和禁用原因；
- `sv_command` 与 Sidebar preview publish 均在创建项目 IPC 请求前执行相同门禁。

## 2. 原生崩溃与恢复证据

在已保存 disposable 工程上复现了 4 次 SynthV 原生崩溃：

| 时间 | 场景 | Windows 事件 |
|---|---|---|
| 15:19:57 | isolated Group-reference clone 后执行 SynthV Undo | `0xc0000005`，offset `0x0000000000bff058` |
| 15:27:23 | 同场景第二次 | 同上 |
| 15:30:58 | 同场景第三次 | 同上 |
| 15:39:37 | Track shell 创建期间/写后回读前 | `0xc0000409`，offset `0x0000000000a1b07d` |

每次崩溃后均重新打开已保存的 `D:\Projects\sv\test.svp`，读取 fresh Session 和目标；没有复用旧 Context/Guard，也没有从 crash 后状态继续写。三个 isolated Undo 事件异常代码和偏移完全一致，可判定为可重复宿主风险。关于响应顺序/垃圾回收的推测性 Lua 改动未改变结果，已完整撤回。

本轮不宣称已修复 SynthV 原生缺陷，而是在 TypeScript 公共能力边界安全收缩。收缩后重放危险 clone/transaction/harmony 请求均未创建项目 request file、未修改工程、未再触发崩溃。

## 3. 最终自动化与静态门禁

| 命令 | 最终结果 |
|---|---|
| `npm run check` | `PASS`：249 pass、0 fail、0 skipped；最新完整测试阶段约 10.197 s |
| `node --check`：clean/install/doctor/release-validation/Stage 3 stability/write-undo | `PASS` |
| Lua 3 文件 `luac54.exe -p` | `PASS` |
| PowerShell Stage 3 脚本（5 个）解析 | `PASS` |
| Window focus 定向回归 | `PASS`：`acquired=true`、`pointerActivationCount=2` |
| API coverage | `PASS`：23 classes、370 methods、215 semantic evidence、64/64 Actions、38 writes、0 errors |
| v3 benchmark | `PASS`：6-tool catalog 4,336 B；64-note Query 4,757 B、p95 0.155 ms；Command ack 129 B、p95 0.002 ms |
| 安装目标 Doctor | `PASS`：运行 MCP capability、Bridge、v3-only 协议、executor、安装文件、Codex 配置和 IPC residual 全部通过；仅 optional Sidebar inactive 警告，磁盘安装内容匹配 |

当前 Executor Build ID：`sv3-lua-0.2.0-alpha.1-f962c134f9f7ef1304c3034d008be2ce588a7ee473a2331de833b3f72917f5aa`。

当前 Sidebar Build ID：`sv3-sidebar-0.2.0-alpha.1-12804fef9a667c30b3bf8724eb256a6a416c91945533a88969f8567fb23b7ad7`。

当前 Node Build Fingerprint：`f6655abd792c49746b411662b1b65ed10e4f83f8f2c874857111daf67923d40b`；Capability Fingerprint：`13bc51b965113951795899148564af778719974b4382b46029d3300f84b61f39`。

运行时、当前编译构建和正确安装目标一致。只读 Doctor 使用脚本根目录 `C:\Users\pengj\AppData\Roaming\Dreamtonics\Synthesizer V Studio 2\scripts` 再次确认 Bridge、Sidebar、协议、MCP 能力和安装文件均匹配，且没有 stale processing/control residual。Benchmark 为 Node synthetic fixture，不包含 SynthV、文件 IPC 或渲染延迟。

## 4. SynthV fixture 与保存基线

Disposable 工程：`D:\Projects\sv\test.svp`。

- 1 Track：`未命名音轨`；Mixer 0 dB、pan 0、mute/solo off；
- main Group 空；1 个非主 Group：`Bridge Stage 1 Fixture`；
- 非主 Group 有 3 个本轮合成测试 note，onset/duration 首尾严格连接，无正间隙或重叠；
- library Group reference count 为 1；Automation points 0；Pitch Controls 0；
- time axis 为 position 0 的 120 BPM、measure 0 的 4/4；
- 播放 stopped，测试选择已清空，工程标题无星号。

所有恢复均使用可见“编辑 → 撤销”菜单，并在恢复后重新 Query。最终工程投影与保存基线一致，无已知遗留 mutation、未处理 Undo 或 `undoRequired=true`。

16:47 在 Codex 与 SynthV 都重启后执行了独立恢复验证：重新打开 `test.svp`、在脚本菜单重新扫描并启动 Bridge，获得新的 Session；`sv_status` 报告 SynthV 2.2.1、protocol v3、executor build 完全匹配且 `writesAllowed=true`。随后重新读取工程，确认 1 Track、空 main Group、非主 Group 的 3 个 note（相邻间隙均为 0）、1 个 library Group/reference、120 BPM、4/4、播放 stopped，窗口标题无未保存星号。带安装目标的 Doctor 再次全部通过；仅 optional Sidebar 未激活，不影响 Bridge 主路径。

17:17 在最后一次 Node 编译身份变化后再次重启/重连 Codex MCP。`sv_status` 报告 Node fingerprint `f60f...f7b8`、executor 和 active Sidebar 均 matched，`writesAllowed=true`；安装目标 Doctor 全项通过。最终只读回读仍为 1 Track、3 notes、library reference count 1、120 BPM、4/4、空选区、无 unfinished edits、播放 stopped。

## 5. Stage 1 结果

| Gate | 结果 | 说明 |
|---|---|---|
| 自动化、语法、API、预算 | `PASS` | 见第 3 节 |
| 连接、v3-only、构建一致性 | `PASS` | SynthV 2.2.1、fresh Session、writesAllowed |
| Query 隐私与预算 | `PASS` | 未发现 UUID/fingerprint/Guard 非预期泄漏 |
| Mixer change/no-op/Undo | `PASS` | no-op 0 Undo；实际写 1 Undo；恢复 |
| Sidebar Preview/Apply/Undo | `PASS` | Preview 不写；Apply 1 Undo；无 Windows 文件竞争错误；恢复 |
| stale/Session change | `PASS` | 旧 Context 写前拒绝，无 Undo |
| shared Group 默认拒绝 | `PASS` | `SHARED_GROUP_WRITE`，写前拒绝 |
| linked clone | `PASS` | 引用增加、源内容不变；一次 Undo 恢复 |
| isolated/Track clone/transaction | `EXPERIMENTAL_DISABLED` | 原生崩溃后安全收缩；最终构建 IPC 前失败关闭 |
| note add/edit/delete/transform | `PASS` | fresh Context、每次 1 Undo、剩余几何连接、恢复 |
| Track add/update/delete | `PASS` | add/delete 各 1 Undo；update no-op/changed/Undo；恢复 |
| Group Reference 增加/update/delete | `PASS` | linked add、mute no-op/changed、delete 均权威回读并 Undo 恢复 |
| Library Group create/delete | `PASS` | 空临时 Group create/delete 各 1 Undo；两次 Undo 恢复；无崩溃 |
| Local score import | `PASS` | 单声部 2-note MusicXML；SHA-256 与权利确认；source tempo 未应用；1 Undo 恢复 |
| Time Axis | `PASS` | 同值 0 Undo；实际变更 1 Undo；回读并恢复 |
| Metadata set/remove | `PASS` | 实际变更 1 Undo；重复操作 0 Undo；回读并恢复 |
| Automation/Vocal tuning/listening | `BLOCKED` | 强制 Vocal/Vocal Mode onboarding 尚未完成 |

由于本 Stage 发生真实宿主原生崩溃，且代表性写 Gate 仍未全部完成，Stage 1 整体只能判为：**安全收缩后的部分通过，保持 Alpha**。

## 6. Stage 2 Query 全覆盖

17/17 Query Action 真机通过。端到端响应均低于普通 20 KB 预算，代表性耗时约 5.7–48.4 ms：

`convert_pitch`、`get_project_info`、`inspect_score_file`、`get_time_axis`、`convert_time`、`list_tracks`、`list_note_groups`、`get_track_notes`、`get_group_voice`、`get_note_phoneme_data`、`get_phrase_context`、`get_computed_group_data`、`get_note_retakes`、`get_pitch_controls`、`get_automation`、`sample_automation`、`get_track_mixer`。

`inspect_score_file` 使用显式绝对路径的单声部 MusicXML 临时文件；未解析 `.svp`，未隐式应用 source tempo。只读 Automation 还确认宿主动态范围可与固定表不同，例如本 Vocal/宿主的 `gender [-2,2]`、`loudness [-48,24]`，因此写入必须使用同一次 fresh read 的 `definition.range`。

## 7. Stage 2 UI 全覆盖

9/9 UI Action 真机通过：selection 读写和清空、editor viewport 改变与恢复、snap、坐标 round-trip、playback seek/play/pause/loop/stop、clipboard 写读恢复、dialog。

首次 dialog 超过 15 s，客户端按设计 timeout 且未自动重试；Bridge 随后恢复。第二次 10.85 s 内点击确认并返回 `result=true`。最终 playback stopped、clipboard/viewport/selection 均恢复。

## 8. Stage 2 写动作状态

已有 fresh 真机证据：

- `set_time_axis`；
- `set_track_mixer`、`add_track`、`update_track`、`delete_track`；
- `add_group_reference`、`update_group`、`delete_group_reference`；
- `create_note_group`、`delete_note_group`；
- `import_monophonic_score`；
- `script_data(set/remove)`；
- `clone_group_reference(linked)`；
- `add_notes`、`edit_notes`、`delete_notes`、`transform_notes`；
- shared Group 默认写前拒绝；
- 7 条危险 clone/transaction/harmony 路径最终失败关闭；
- Sidebar preview/apply 的等价 guarded mixer 写。

后续 Vocal onboarding 已完成；Retake、Smart Pitch、Automation、phoneme、humanize/expression/lyrics 和综合 tuning 均获得 fresh 真机 changed/readback/Undo 证据，详见第 11–12 节。机器可读 Action 清单最终为 31 个 `verified`、7 个 `experimental`、0 个 `pending`；不存在 `unsupported`。

Stage 3 尚未开始。设计要求先完成 Stage 2（包括人工听感），再执行 1,000 mixed reads、200 write/Undo、缩减能力集的 clone/transaction fail-closed、reload/concurrency 重复矩阵和四小时持续运行。read-only driver 已可执行并通过一次 17-call 真机开发 smoke，但在没有显式 `--stage2-complete` 时会在连接前拒绝 1,000-read live matrix，因此不能把 harness smoke 冒充 Stage 3 证据。

## 9. Vocal 门禁（已完成）

调音写入前要求：

1. 在 SynthV 中选中目标 Note Group；
2. 为该 Group 选择或分配 intended Vocal；
3. 提供完整 Vocal Mode 面板截图，或逐字输入面板中的每个 Vocal Mode 名称，保留拼写和大小写；
4. 指定 intended style、允许改变的内容和必须保留的内容，并先确认 phrase-level 风格。

用户随后按第 11 节完整提供并完成了这项强制门禁。官方脚本 API 仍无法读取当前 Vocal 身份，也无法枚举从未改动、仍为默认值的全部 Vocal Mode；更换 Vocal 后仍必须重新提供完整面板，不能复用旧名单。

## 10. 17:17 阶段状态与当时的发布判断

- 工程已恢复到保存基线，无已知遗留 mutation；
- Bridge executor 和 active Sidebar 在最新 Session 运行且与源代码一致；Codex 内置 MCP 旧连接已关闭，等待任务重启后重新建立；
- 246/246 自动化、17/17 Query、9/9 UI、31/31 支持的 write Action 通过；
- 危险 clone/transaction/harmony 公共路径已失败关闭；
- 真机崩溃证据、待确认的人工听感和 Stage 3 仍存在；
- 未 commit、未 push、未开 PR、未发布 stable。

最终决策：**保持 `0.2.0-alpha.1`。完成人工听感和 Stage 3 证据前，不得宣称 full 或 reduced stable。**

## 11. 17:18–17:42 Vocal 门禁解除、调音写入与 Automation 缺陷闭环

用户已完成强制 Vocal 门禁：目标为 Track 1 / Group 2 `Bridge Stage 1 Fixture`，已选择 intended Vocal，并提供完整 Vocal Mode 面板截图。逐字确认的名称为 `Airy`、`Bright`、`Cool`、`Dark`、`Emotional`、`Power`、`Solid`、`Sweet`。技术验证风格定义为中性清晰、略带 Airy/Bright；仅允许对本任务创建的 fixture 做临时 Voice/Vocal Mode、phoneme、Retake、Smart Pitch、Automation、humanize/expression/lyrics 改动，最终必须恢复 3 notes、歌词、pitch、duration、内部 gaps `[0,0]`、Vocal、Mixer 和 time axis。

本轮已获得 fresh 真机 changed/readback/Undo 证据：

- `set_group_voice`：写入 tension `0.1` 及 `Airy` pitch/timbre/pronunciation `10/5/5`，单 Undo、后置验证通过；菜单 Undo 后 Voice 参数和 Vocal Modes 回到未写入状态；
- `set_note_phoneme_properties`：第 1 note 的 even-syllable 与两个 phoneme attributes 真机写入、回读、单 Undo 恢复；
- `fit_lyrics`：3 notes 临时改为“明/亮/声”，几何不变，单 Undo 后恢复 `la/la/la`；
- `humanize_notes`：只改变最后一个 note 的尾长，不改变 onset，因此两个内部 gaps 始终为 0；单 Undo 恢复原 duration；
- Retake：`generate_note_retake`、`activate_note_retake`、`delete_note_retake` 依次通过，最终 count 回到 1；随后用三次可见菜单 Undo 清空整条测试历史；
- Smart Pitch：`add_pitch_controls`、`edit_pitch_controls`、`delete_pitch_controls` 依次通过，最终 count 回到 0；随后用三次可见菜单 Undo 清空整条测试历史。

Automation 首次使用 fresh `tension` 定义范围 `[-2,2]` 写入 7 个线性点时，`set_automation_points` 在真实写入后误报 `HOST_POSTCONDITION_FAILED`，返回 `wrote=true`、`undoRequired=true`。严格遵守恢复协议，在任何复读或重试前只执行一次 SynthV Edit > Undo；随后复读确认曲线仍为 0 points，窗口标题无星号。最小化 Fake Host 回归只需一个 `0.1` 点即可稳定复现：实际点数与期望点数都为 1，但宿主将值保存为 float32，而旧后置条件以 JSON 精确相等比较 double。真机单变量探针 `0.125` 在旧实现上通过，确认根因不是步进量或自动简化。

修复增加严格 Automation 等价规则：point count 和 position 必须完全一致；value 只接受原值本身或该值精确的 float32 往返结果。该规则同时用于 no-op 判定、独立 set/simplify/clear 后置条件及 `apply_group_tuning` 聚合后置条件，不会容忍更大的宿主偏移。新增 `CMD-006: Automation postconditions accept host float32 normalization` 回归先红后绿；完整 `npm run check` 为 **239/239 pass、0 fail、0 skipped**。

修复版 executor 已安装并由 SynthV 热加载，新的 Executor Build ID 为 `sv3-lua-0.2.0-alpha.1-f962c134f9f7ef1304c3034d008be2ce588a7ee473a2331de833b3f72917f5aa`。当前 Codex MCP 进程仍缓存旧期望 ID `...366cf71e...`，因此构建一致性门禁正确报告 `mismatch`、`writesAllowed=false`。继续真机复测前必须重启 Codex，使 Node 端加载新的构建元数据；此状态不是工程 mutation，SynthV fixture 当前仍为保存基线且标题无星号。

## 12. 17:42–18:15 修复版端到端复测、综合调音与恢复

为避免把 Codex 内置 MCP 的旧构建缓存误当作 Bridge 缺陷，本轮启动了一个独立的当前构建 MCP 客户端。该客户端仍严格经过公开的六工具 v3 门面、Context/Guard 展开、文件 IPC 和 SynthV Lua executor；状态检查报告 Node 与 executor 均为当前构建，`writesAllowed=true`。它不是绕过协议直接调用 Lua。

Automation 修复后的真机链路完整通过：

- 对 fresh `tension` Automation 定义范围 `[-2,2]` 写入 7 个点，宿主回读为 float32 值（例如 `0.10000000149012` 与 `0.60000002384186`），后置条件正确接受且没有放宽 position 或 point-count 校验；
- `simplify_automation` 将 7 点简化为 3 点，`clear_automation` 将其清为 0 点；
- 三个动作均报告 changed、各自一个 Undo、独立回读通过；随后通过三次可见 SynthV Undo 清除整条测试历史，标题恢复为无星号。

其余调音写动作真机结果：

- `apply_expression_preset`：为 3 个测试 note 写入 vibrato strength `0.5`，仅 note attributes 变化，onset、duration、pitch 和两个内部 gap 均保持；一个 Undo，独立回读通过，随后可见 Undo 恢复；
- `apply_group_tuning`：在一次聚合调用中写入 Group Voice tension `0.1`、Vocal Mode `Bright` 的 pitch/timbre/pronunciation `8/4/4`、第 1 note 的 phoneme 属性与 even-syllable、第 3 note detune `5`、3 个 tension Automation 点，以及 1 个 Smart Pitch 点（pitch `0.25`）；完整批次只创建一个 Undo Record，各域独立 Query 均验证通过，note 几何保持不变；
- 调音后使用 `sv_ui playback` 在 `0..1.5 s` 循环播放，宿主实际报告 `looping`；随后已停止播放。机器侧只能确认播放链路和状态，**听感仍等待用户确认，不能计为 human listening PASS**；
- 对聚合调音执行一次可见 SynthV Undo 后，最终独立基线审计确认：3 notes，pitch `[60,62,64]`，duration 均为 `705600000` blick，gaps `[0,0]`，歌词恢复为 `la/la/la`，Voice/Vocal Mode/phoneme 临时值均清除，Automation 0 点，Pitch Control 0 点，播放 stopped，窗口标题无星号。

更新后的覆盖清单为 **31 个 verified write、7 个 experimental、0 个 pending**。七个 experimental 路径仍在项目 IPC 前 fail closed，不以原生崩溃路径换取覆盖数字。RetakeList 的真机证据也已从 pending 更新为 sampled。

本轮最终静态与自动化门禁：

- `npm run check`：239/239 pass，0 fail，0 skipped；
- `node --check`：clean、install、release driver 均通过；
- Lua 3 文件由 Lua 5.4.8 `luac54.exe -p` 检查通过；
- API coverage：23 classes、370 methods、215 条证据、64/64 Actions、38 writes、0 errors；
- v3 benchmark：六工具 catalog 4,336 B，64-note Query 4,757 B / p95 0.168 ms，Command ack 129 B / p95 0.002 ms；
- `git diff --check` 通过。

安装目标 Doctor 当前唯一非 `OK` 项是两条 MCP `WARN`：Codex 内置 MCP 旧连接已关闭，必须由 fresh running MCP 重新写入能力心跳；Bridge executor、Sidebar 和安装文件本身均为当前构建，Doctor 退出码为 0。尝试只终止旧 Node MCP 子进程后，内置工具显示 `Transport closed` 且未自动拉起。因此该项必须由真正的 Codex 任务重启/重连重新建立，不伪造 heartbeat，也不影响上面由当前独立 MCP 客户端完成的端到端真机证据。

Stage 2 机器侧写入矩阵现已完成；当时仅 human listening 判定待用户返回。Stage 3 尚未宣称通过：1,000 mixed reads、200 write/Undo、并发、重载与四小时持续运行等正式门禁仍需在听感通过后执行；已被禁用的危险 clone/transaction 路径不得为满足次数而重新调用。

## 13. 17:56–18:08 Stage 3 可执行性开发与真机 smoke

发布设计要求 tracing on/off p95 对照，但旧实现只有始终收集 Trace，没有可控的关闭基线。本轮新增默认开启的 `SYNTHV_AGENT_TRACE_ENABLED` 开关；仅当独立验证进程显式设置为 `0`、`false` 或 `off` 时关闭有界内存 Trace 收集，不改变六工具 MCP、文件 IPC、宿主调用或工程状态。新增回归覆盖开关解析。

新增 `validate:v3-stability` 驱动，支持：

- 200 个并发 Node 请求，校验请求无丢失、每个结果有独立 trace ID，且工程/Session/executor 不漂移；
- 30 次 Bridge reload 和 30 次旧 Context 写前失效；
- 缩减能力集的失败关闭矩阵：每条 experimental 能力重复 30 次，两类 dependent transaction 形状各 100 次，均必须在项目 IPC 前返回 `EXPERIMENTAL_CAPABILITY_DISABLED`、`undoRequired=false`；
- 独立 MCP 进程的 Trace off/on A/B，预热后比较真实宿主 p95，正式阈值小于 5%；
- formal count 前强制 `--stage2-complete`，低计数只能标记 `developmentSmoke`。

第一轮真机 harness smoke 结果：10 个并发请求全部返回、10 个 trace ID 唯一、0 request loss；8 类危险形状各 2 次，共 16 次全部失败关闭；1 次 Bridge reload 产生 fresh Session，旧 Context 在状态轮询已清空后正确返回 `UNKNOWN_CONTEXT`。重载驱动最初只接受 `SYNTHV_SESSION_CHANGED`，因此 smoke 首次失败；诊断确认状态轮询已经先消费 Session 变化并清空 Context，随后修复为分别计数并接受 `SYNTHV_SESSION_CHANGED` 或 `UNKNOWN_CONTEXT`，复测通过。该过程没有工程写入或 Undo。

Trace A/B smoke 每个状态 10 个测量样本：off p95 `46.387 ms`，on p95 `47.576 ms`，增量 `2.561%`，低于 5% 阈值。计数不足正式 100/状态，所以只证明驱动可用，不计为 Stage 3 性能通过。

新增驱动及其写计划回归后的完整门禁为 **246/246 tests pass、0 fail、0 skipped**；API coverage 仍为 23 classes、370 methods、215 条证据、64/64 Actions、38 writes、0 errors；Node 语法、Lua 5.4.8 三文件编译和 `git diff --check` 均通过。最新 synthetic benchmark 为 Query p95 `0.169 ms`、Command p95 `0.002 ms`，公共体积预算不变。

## 14. 18:09–18:25 人工听感、保存基线校正与正式 Stage 3 非破坏性矩阵

用户明确回复“听感通过”。监听场景使用与机器验证一致的中性清晰、略带 Bright 的综合调音，播放状态由宿主报告为 looping；确认后先通过 `sv_ui` 停止，再在 SynthV 主窗口执行且只执行一次 `Ctrl+Z`，播放恢复 stopped。后续 Computer Use 截图纠正了最初的目视判断：窗口标题仍为 `test.svp*`，不能声称工程已回到保存点。

Undo 后的更细只读审计纠正了第 12 节对基线的过度简化：当时工程含 Voice tension `0.1`、`Bright 8/4/4`、第 3 note detune `5` 和 1 个 Smart Pitch point；note 数、pitch、duration、歌词和 gaps `[0,0]` 保持，Mixer/time axis 未变。`test.svp` 文件最后写入时间为 16:09:54，但标题仍有星号，因此没有把当时的内存状态误认成保存基线，后续 SynthV 写入暂停并等待用户确认。

18:29 用户明确授权丢弃未保存更改。通过 SynthV 的“打开”工作流重新载入 `D:\Projects\sv\test.svp` 后，窗口标题稳定为 `test.svp`（无星号），Bridge 产生新 Session `1785514770-5911550000-167219`，状态 fresh/matched、`writesAllowed=true`，executor/sidebar build 均与当前源码匹配。重新载入后的磁盘基线成为后续恢复权威：1 Track、Group 2 `Bridge Stage 1 Fixture`、3 个连续 note（pitch `60/62/64`、各 `705600000` blick、gaps `[0,0]`、歌词均为 `la`）、Mixer `0 dB/0/false/false`、120 BPM、4/4、playback stopped；保存文件本身包含 Voice tension `0.1`、Bright `8/4/4`、第 3 note detune `5`、1 个 Smart Pitch point、note 1 phoneme attributes，以及 3 个 tension Automation points。后续每轮写测试都必须以该已保存状态为恢复锚点。

听感门禁通过后执行正式非破坏性 Stage 3：

- 1,000/1,000 mixed reads：17 Query 全覆盖；最大响应 1,700 B / 1,690 characters；p50 `34.456 ms`、p95 `47.447 ms`、maximum `62.323 ms`；工程、Session、executor 和响应预算无漂移。首次启动在 0/1000 前因瞬时 stale heartbeat fail-fast；只读诊断恢复 fresh/matched 后从 0 重跑并完整通过；
- 200/200 concurrent Node requests：0 request loss，200 个唯一 trace ID。首次通过时 SDK 发送端因并发 backpressure 超过默认 10-listener 警告阈值；驱动改为仅给该 MCP 子进程 stdin 设置 `count+16` 的有限监听预算，复跑 200/200 无警告通过；总耗时 `6.185 s`；
- 380/380 reduced-capability fail-closed calls：6 类禁用动作各 30 次、两类 transaction 形状各 100 次；全部在项目 IPC 前拒绝，0 unexpected writes；
- 30/30 Bridge reload 与 30/30 Session invalidation：旧 Context 全部在写前返回 `UNKNOWN_CONTEXT`；p95 `56.384 ms`；
- Trace A/B：off/on 各 100 个测量样本，p95 `48.076/48.155 ms`，增量 `0.164%`，通过 `<5%` 门禁。

Stage 3 当时尚未整体通过：仍需 200 ordinary write/Undo（每个支持动作至少 3 次）、30 linked clone/Undo，以及至少四小时 mixed read/write/Undo/reload/idle heartbeat 长稳。SynthV 官方 API 只能创建 Undo Record，不能执行 Undo；剩余矩阵必须保留可见、逐次验证的主编辑器 Undo 工作流。后续完成情况见第 16 节。

## 15. 18:25–18:26 未保存状态保护与写矩阵计划

准备重新打开保存工程以清空旧 Undo 栈时，最新 Computer Use 截图确认标题仍为 `test.svp*`。根据本仓库的 stop-first 规则，所有 SynthV 写入立即暂停；在用户明确确认前，不重开、不保存、不继续 Undo，也不把仍含星号的状态称为恢复基线。

等待确认期间只修改仓库：新增 31 个 `verified` write Action 的权威 Stage 3 注册表和 200-call 调度器。默认分配为前 14 个 Action 各 7 次、其余 17 个各 6 次，全部高于每 Action 至少 3 次；少于 93 次的计划会在生成时拒绝。`validate:v3-stability -- --dry-run --mode all` 现在同时输出该分布、200 ordinary write/Undo 和单独 30 linked-clone/Undo 要求，并明确标记必须使用可见 SynthV Undo。计划回归已通过，尚未冒充真机写入证据。

## 16. 18:29–19:45 保存基线恢复、正式写/Undo 矩阵与四小时耐久测试启动

用户明确允许丢弃未保存更改后，关闭 SynthV 并重新打开磁盘中的 `test.svp`，在自动恢复提示中选择“取消”，从而排除 scratch/recovery 状态。重新读取的保存基线与第 14 节记录一致。为保留真实宿主的 Undo 语义，新增 `stage3-visible-undo-loop.ps1`：每轮先执行一个 fresh-context 写动作，再通过 Windows 主窗口可见“编辑 → 撤销”菜单执行且只执行一次 Undo，随后重读完整工程摘要并与准备摘要比较；任何写入、Undo、摘要或 pending 状态异常都会立即停止。

正式 ordinary write/Undo 矩阵结果为 **200/200 PASS**：覆盖全部 31 个 `verified` 写动作，前 14 个动作各 7 次、其余 17 个动作各 6 次。每次写入均报告 changed、创建且只创建 1 个 Undo Record；每次可见 Undo 后完整摘要均恢复为 `73212a577edd237b50695fad7ac1d29a599bd1a99059c2f2beeddbbb6b6b7393`，`pending=null`。首轮完成于 `17:13:25.362Z`，末轮完成于 `17:26:06.932Z`。期间没有复用 stale Context/Guard，也没有把 no-op 计为成功写入。

随后为稳定支持的 `clone_group_reference(linked)` 增加独立模式和 fresh source/target 准备逻辑。正式 linked clone/Undo 矩阵结果为 **30/30 PASS**：每次 linked clone 均改变 1 个引用、创建 1 个 Undo Record；每次可见 Undo 后恢复同一完整摘要且 `pending=null`。首轮完成于 `17:30:49.312Z`，末轮完成于 `17:32:49.878Z`。`isolated` clone 继续保持 IPC 前失败关闭，没有为了计数重新启用已证明会导致原生崩溃的路径。

为覆盖持续时间、空闲间隔和跨轮次生命周期，新增 `stage3-four-hour-soak.ps1`。正式参数为 4 小时、200 个间隔写/可见 Undo、每轮 17 Query（计划合计 3,400 reads）、每 20 轮一次 Bridge reload（计划 10 次），并在整个进程中阻止系统睡眠；写后、Undo 后、查询后及重载后均校验 Session、executor、工程摘要和 pending 状态。1-write/17-read/1-reload 的短 smoke 已通过。

复核发布设计时发现主驱动尚未保存资源曲线证据，因此补充只读伴随驱动 `stage3-resource-monitor.ps1`，而未重启或修改正在运行的主测试。它每分钟记录 SynthV working set/private bytes、handle/thread 数、Node 总工作集、Bridge heartbeat 年龄和 processing/control residual；每 20-write/reload 批次后另取一个 settled sample。最终要求前 5 个 regular samples 的中位数作为稳定基线、10 个批次样本和最终样本均不超过基线 120%、heartbeat 不超过 5 秒、没有超过 30 秒的 processing/control residual。伴随监控从主测试第 8 轮开始，早于第一个 20 轮批次；其脚本解析与仓库 whitespace 检查通过。

正式四小时运行于 `2026-07-31T17:43:11.9311756Z` 启动，计划截止 `21:43:11.9311756Z`。截至本节写入时，前 3 轮（51 reads）连续通过，Undo 后摘要始终一致，结果文件尚未生成，主监控进程仍在运行。本项在达到完整时长和全部计数前只记为 **RUNNING**，不提前计为 Stage 3 PASS。

资源监控的首个正式批次覆盖第 20 轮与第 1 次 reload。前 5 个 regular samples 的 settled 中位数为 working set `549,179,392 B`、private bytes `577,302,528 B`；完成批次后等待 60 秒的样本为 `607,334,400 B` / `634,413,056 B`，比率 `1.105894` / `1.098927`，均低于 `1.2`。Heartbeat age `507 ms`，无 processing/control residual。早期约 300 MiB 的样本属于 warm-up 前状态，不能替代已声明的五样本中位数；中间瞬时峰值和随后回落均保留在原始 JSONL 中。

## 17. 20:14–20:19 首次长稳 fail-fast、可见 Undo 前台竞争与修复

首次正式四小时运行在第 27 次写入后按设计停止：此前 26/26 writes、442 reads、1 reload 全部通过；第 27 个 `fit_lyrics` 报告 changed、1 Undo Record、verified，但自动可见 Undo 后完整摘要仍为 `4895f280...`，未恢复准备摘要 `73212a57...`。运行在 `1,877.293 s` 处停止，没有继续下一次写入。State 保留 `pending.action=fit_lyrics`、`pending.index=27` 和写前摘要；没有自动重试或额外 Undo。

只读诊断确认目标 note 仍存在、歌词字段仍为字符串且长度为 1，而保存基线长度为 2；诊断输出没有打印歌词内容。Computer Use fresh 截图显示 SynthV 工程仍处于未保存状态；通过真实“编辑 → 撤销”菜单执行一次且只执行一次 Undo 后，SynthV 进入短暂“渲染歌声”状态。权威摘要验证随后恢复为 `73212a577edd237b50695fad7ac1d29a599bd1a99059c2f2beeddbbb6b6b7393`，pending 清除，第 27 轮正式标记恢复。

根因不在 `fit_lyrics`、Bridge Undo Record 或 SynthV Undo，而在 Windows runner：它调用 `SetForegroundWindow` 后忽略返回和真实 foreground HWND。当其他窗口持有输入权时，第一个坐标点击只激活 SynthV、没有打开“编辑”菜单，第二个坐标点击随即落入编辑区。修复后 runner 会 restore/foreground SynthV，必要时用无副作用标题栏点击取得输入，再用 `GetForegroundWindow` 在打开菜单前、打开菜单后和执行 Undo 后三次核验同一 HWND；无法核验时在点击 Undo 前失败关闭。

为了真机复现而非只依赖代码推断，先显式把文件资源管理器置前，再用修复后的 runner 执行第 28 个 `delete_notes` 单轮 smoke。Runner 成功夺回 SynthV 前台、命令 changed/1 Undo/verified、一次可见 Undo 后摘要恢复为同一准备摘要，`pending=null`。脚本解析、12/12 Stage 3 定向回归和 `git diff --check` 均通过。首次 31 分钟运行作为失败与恢复证据保留，**不得计为四小时持续运行 PASS**；完整计时必须从保存基线重新开始。

修复后的完整仓库门禁为 **248/248 tests pass、0 fail、0 skipped**，最终测试阶段约 `9.35 s`。该次门禁在 SynthV 自动恢复提示保持未操作、没有真机 runner 运行时执行，因此没有与项目 IPC 或可见 Undo 竞争。

## 18. 20:31 起第二次长稳、用户窗口移动恢复与同一时间线续跑

从磁盘重新加载 `test.svp`、取消自动恢复并确认标题无星号后，第二次正式四小时运行于 `2026-07-31T18:31:53.5982814Z` 启动，原计划截止 `2026-07-31T22:31:53.5982814Z`。Bridge Doctor 在启动前确认协议仅 v3、Lua executor build 为 `sv3-lua-0.2.0-alpha.1-f962c134f9f7ef1304c3034d008be2ce588a7ee473a2331de833b3f72917f5aa`、Node build/capability fingerprint 与当前编译产物一致、无 processing/control residual。

第 9 个 `set_group_voice` 写入报告 changed、1 Undo Record、verified 后，runner 在点击 Undo 前因无法确认 SynthV 前台 HWND 而 fail closed；前 8 轮和 136 reads 已通过，State 保留 `pending.index=9`、`pending.action=set_group_voice` 和写前摘要。用户随后说明当时移动过 SynthV 窗口，并要求不要从头重跑。一次发送到 SynthV 的 `Ctrl+Z` 没有改变工程摘要，证明该快捷键没有触发实际 Undo；随后通过可见“编辑 → 撤销”菜单执行一次真正的 Undo，权威验证恢复摘要 `73212a577edd237b50695fad7ac1d29a599bd1a99059c2f2beeddbbb6b6b7393`，`pending=null`，没有第二次实际 Undo。

为把瞬时焦点竞争变成确定性回归，抽出 `stage3-window-focus.ps1`，用可注入 foreground 序列模拟第一次真实激活输入仍被竞争窗口夺走、第二次才成功。生产等价重构保持单次尝试时，`stage3-window-focus-regression.ps1` 稳定报错 `Transient foreground contention was not recovered before visible Undo.`；把策略改为最多 3 次有界尝试后，同一回归返回 `acquired=true`、`pointerActivationCount=2`。每次尝试都重新读取 SynthV 窗口矩形，因此移动窗口不会复用旧坐标；菜单打开前、打开后和 Undo 后仍校验同一 HWND，无法确认时仍在实际 Undo 前停止。PowerShell 解析、重新构建后的 12/12 Stage 3 定向测试均通过。

主驱动新增显式 `-Resume` 恢复模式：只接受 `pending=null`、相同准备摘要、零 linked-clone 计数，且 runtime state 最多只能比 JSONL 最后一轮领先 1 次；它保留原始 `startedAt`/`deadline`，把已经人工恢复的第 9 轮补齐 17 reads 并写入 `recoveredAfterInterruption=true`，随后从第 10 轮继续。它不会重置计数或把缺失 evidence 静默跳过。续跑追赶原时间表后恢复 72 秒间隔；截至 `2026-07-31T19:00:47.5206886Z`，25/200 writes、425 reads、1/10 reload 均通过，每轮一次实际 Undo 后摘要一致且 `pending=null`。本项仍为 **RUNNING**，最终结果必须同时披露该用户交互中断和恢复事件。

资源曲线显示一次性冷加载与持续增长必须分开判断。中断前约 284 MB 的冷基线在首次 Voice/完整 tuning 渲染后暂态升至 working set/private bytes `587,579,392 B` / `613,933,056 B`；约两分钟后回落到约 `320 MB` / `350 MB`，不支持持续泄漏推断。冷加载 JSONL 已另存保全；只重启只读资源监控而未重启主 soak，以已预热状态继续覆盖全部 20-write/reload 批次。五个预热 regular samples 的 settled 中位数为 `320,593,920 B` / `352,129,024 B`；第 20 轮 settled batch 为 `320,376,832 B` / `350,367,744 B`，比值 `0.999323` / `0.994998`。当前最大 heartbeat age `1,020 ms`，无 residual。发布设计本来就要求 `After warm-up`；驱动现将冷样本继续记录用于诊断，但默认只有完成至少 10 次写入后的 regular samples 才能进入五样本 settled baseline，并用“已完成的最大 20 倍数”发现批次，避免只读监控重启时漏掉已越过的批次。最终仍需 10 个批次、非单调增长和最终样本共同判定，不能用当前单批次提前宣称 PASS。

## 19. 21:31–21:43 一小时高密度续跑、最终恢复与发布判定

用户随后明确要求把 Stage 3 总时长缩短为一小时，并通过提高密度在原始起点之后继续，而不是启动第二套测试。主驱动增加 `-OverrideDurationOnResume`：只有恢复时显式给出该参数，才把截止时间改为原始 `startedAt + DurationHours`；默认恢复语义仍保留原截止时间。最终沿同一时间线从 `2026-07-31T18:31:53.5982814Z` 运行至 `19:31:54.3652180Z`，实际时长 `3,600.767 s`。

功能主结果为 **PASS**：`200/200` writes、`3,400/3,400` reads、`10/10` reloads 全部完成；前 14 个 verified Action 各 7 次、其余 17 个各 6 次。每次写入后只执行一次可见 Undo，最终 `pending=null`，准备摘要与恢复摘要均为 `73212a577edd237b50695fad7ac1d29a599bd1a99059c2f2beeddbbb6b6b7393`。两次 transient fresh-baseline 读取失败分别发生在高密度续跑第 64 和 186 轮：写入、可见 Undo 和摘要恢复都已完成，只读 baseline 子进程没有及时建立 fresh 连接。驱动仅对该明确的只读错误增加最多 3 次、每次间隔 750 ms 的有界重试；不重试写入、Undo 或旧 Context。原始失败结果、stderr 和恢复记录均单独保全，最终只补齐缺失读取后继续，没有伪造样本或启动第二套时间线。

资源伴随门禁按原规则判为 **FAIL/不完整**，不得与功能主结果混为一谈：44 个 regular samples、5 个 baseline samples、9 个 batch samples（监控进程恢复期间漏掉 batch 40，未补造）；预热基线 working set/private bytes 为 `320,593,920 B` / `352,129,024 B`，规定最终采样窗口的比率为 `2.471113` / `2.338203`，高于 `1.2`，最大批次比率为 `2.535390` / `2.338203`。没有单调增长、最大 heartbeat age `1,020 ms`、无 missing heartbeat 或 stale residual。结束约 148 秒后的额外只读诊断样本已回落至 `333,717,504 B` / `355,635,200 B`（比率 `1.040935` / `1.009957`），说明存在延迟回收，但它不替代原规定窗口，也不能把资源门禁改判为 PASS。

用户在最终恢复提示中明确选择“放弃更改”。随后从磁盘重新打开 `D:\Projects\sv\test.svp`，确认标题无未保存星号；停止并重新启动 Bridge 后获得 fresh Session。最终只读验收确认协议仅 v3、组件 matched、`writesAllowed=true`，工程为 1 Track、1 个非主 Group/reference、3 个相连 notes、120 BPM、4/4、播放 stopped，且没有 pending/unfinished edit。安装目标 Doctor 通过；仅 optional Sidebar inactive 警告，磁盘安装内容匹配。

最终门禁：`npm run check` **249/249 pass**、0 fail、0 skipped；6 个 Node 脚本语法检查、3 个 Lua 5.4.8 文件编译检查和 5 个 Stage 3 PowerShell 脚本解析全部通过；window-focus 定向回归、API coverage 与 v3 benchmark 均通过。综合结论仍为 **`0.2.0-alpha.1` / Alpha**：一小时高密度功能耐久通过，但原定四小时时长没有执行，资源门禁在规定窗口内失败，且历史原生崩溃证据仍要求危险 clone/transaction 路径保持 `experimental` 并在 IPC 前失败关闭，因此当前证据不支持 stable。本节是第一次资源门禁后的历史判断，已被第 20 节的修复、用户豁免和最终发布决策取代。

## 20. 21:43–23:40 资源监控修复、用户取消重测与 0.2.0 决策

为消除第一次资源曲线的监控启动时差，随后启动了一套同步的一小时 Stage 3：功能主驱动再次完成 `200/200` writes、`3,400/3,400` reads 和 `10/10` reloads，最终 `pending=null`、`undoRequired=null`、准备摘要与恢复摘要一致。资源监控却在第 200 轮最终检查点崩溃：PowerShell `[Math]::Max` 对大毫秒值选择了 `Int32` 重载，同时一次瞬时无效文件时间戳越过了旧校验。该尝试的原始结果、JSONL 和 stderr 已保存在 `%TEMP%\synthv-stage3-release-attempt1-failed-20260731T211435Z`；它证明功能耐久再次完成，但因缺少最终资源检查点，不计资源 PASS。

提交 `b8e39b4` 修复了两个监控缺陷：文件年龄只接受合理时间戳并有界重读，数值下界显式使用 `double`，同时新增 `-SelfTestFileAge` 与仓库回归。修复后 `npm run check` 为 `250/250 PASS`，文件年龄自检通过，PR #7 的 Node 20、Node 22 和 Lua CI 全绿。正式同步重测随后启动，但用户在 15 个干净循环后明确指示“不用重测了，继续下一步”；两个进程均被安全停止，状态仍为 `pending=null`、`undoRequired=null`，没有再发起第三套或并行测试。

用户再次明确确认“放弃更改”。SynthV Stage 3 草稿已通过可见界面放弃，从磁盘重新打开 `D:\Projects\sv\test.svp`；窗口标题为 `test.svp`、无星号。Bridge 停止并重启后产生 fresh Session，协议仍仅 v3，工程保持 1 Track、1 个非主 Group/reference、3 个相连 notes、120 BPM、4/4、播放 stopped，没有 pending edit。

稳定版准备把所有组件版本统一为 `0.2.0`。最终本地门禁结果：Node `v24.18.0` / npm `11.16.0` 下 `npm run check` 为 **250/250 PASS**（0 fail、0 skipped）；6 个 Node 发布脚本语法检查、3 个 Lua 5.4.8 文件编译检查、6 个 PowerShell 脚本解析及文件年龄自检全部通过；API coverage 为 64/64 Actions、38 writes、0 errors；v3 synthetic benchmark、34 次 read dry-run 和完整 stability dry-run 均通过。GitHub PR #7 在提交 `b8e39b4` 上的 Node 20、Node 22 和 Lua 检查全部成功，最终版本提交仍须由同样 CI 再确认。

最终决策为 **`0.2.0` reduced stable**。依据是公开稳定面只包含 31 个已完成真机、Undo、重复性和听感验证的写动作；7 个已复现原生崩溃风险的 clone/transaction/harmony 路径继续在 TypeScript 边界、文件 IPC 之前失败关闭，未被重新启用。资源门禁的最终状态是 **`WAIVED / NOT PASS`**：用户接受该已披露风险并取消再次一小时重跑；这不是资源通过声明，也不改变实验路径的终态。npm 包保持 `private: true`，本次只发布 GitHub tag/Release，不发布 npm。
