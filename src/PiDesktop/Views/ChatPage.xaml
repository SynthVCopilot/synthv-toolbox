<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PiDesktop.Views.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0">
            <ItemsControl x:Name="MessagesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,4" Padding="12,8" CornerRadius="8"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="{Binding Role}" Opacity="0.6" FontSize="12" />
                                <TextBlock Text="{Binding Content}" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="1" Padding="12" CornerRadius="8"
                Background="{ThemeResource SubtleFillColorSecondaryBrush}">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="转换或平衡响度会生成新文件，并需要你在音频准备页面确认。"
                           VerticalAlignment="Center" TextWrapping="Wrap" />
                <Button Grid.Column="1" Content="处理音频" Click="OpenAudioPreparationButton_Click" />
            </Grid>
        </Border>

        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="InputBox" Grid.Column="0" PlaceholderText="和 Pi Agent 说点什么…"
                     AcceptsReturn="True" KeyDown="InputBox_KeyDown" />
            <Button x:Name="SendButton" Grid.Column="1" Content="发送" Click="SendButton_Click"
                    Style="{ThemeResource AccentButtonStyle}" />
        </Grid>
    </Grid>
</Page>
