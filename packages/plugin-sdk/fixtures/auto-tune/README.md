# Auto Tune fixture

This folder is an installable local plugin. Select the folder itself in the Plugins page, or package its contents into a ZIP with `manifest.json` at the archive root. It declares a sandboxed GUI page and a backend entry point; neither imports Tauri.
